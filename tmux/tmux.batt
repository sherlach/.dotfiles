#!/bin/bash

#chmod +x and move the script into $PATH

# This script returns battery percentage on my device (used for lazy tmux)
echo $(cat /sys/class/power_supply/BAT1/capacity)"% ("$(cat /sys/class/power_supply/BAT1/status)")"
